import { assert, describe, expect, it } from 'vitest';

import { shrine_backend_anony, shrine_backend_authed } from './actors';



describe('trove', () => {
    it('does_nothing', async () => {
        expect(1).toEqual(1);
    });
})


